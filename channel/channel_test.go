package main

import (
	"testing"
)

func TestAddUser(t *testing.T) {
	// server := NewServer()
	// server.Start()

	// for i := 0; i < 10; i++ {
	// go func(i int) {
	//server.userch <- fmt.Sprintf("User_%d", i)
	// }(i)
	// }
	// fmt.Println("the loop is done")
}
